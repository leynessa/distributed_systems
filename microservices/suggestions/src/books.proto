syntax = "proto3";

package books;

service BookService {
  rpc GetSuggestions (BookRequest) returns (BookList);
}

message BookRequest {
  // Define request fields here
}

message BookList {
  repeated Book books = 1;
}

message Book {
  string title = 1;
  string author = 2;
}